# Copy to .env.local (or set the same names in the Vercel project settings).
# Values come from Supabase → Project Settings → API.
# Leave blank until you have a live project — do not invent a URL or key.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
