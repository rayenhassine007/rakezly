// Single place to paste Supabase credentials for this static site.
// Leave empty until you have a *live* project — do not invent a URL/key.
//
// The previous project ref `kucqirnkgrtebmowzwlw` is NXDOMAIN (gone). Email /
// Google login cannot work until you restore or create a project and fill
// these in (or set Vercel env vars). See supabase/SETUP.md.
//
// Option A (simplest): paste Project URL + publishable/anon key below.
// Option B (Vercel / local Vite): set VITE_SUPABASE_URL and
//   VITE_SUPABASE_ANON_KEY; vite.config.js injects them at serve/build time.
window.RAKEZLY_SUPABASE = {
  url: '',
  key: ''
};
