# Dependencies (Vercel reinstalls these automatically)
node_modules/

# Build output (Vercel rebuilds this)
dist/

# Local secrets / overrides (use .env.example as the template)
.env
.env.local
.env.*.local

# Vite temp files
vite.config.js.timestamp-*.mjs

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/
